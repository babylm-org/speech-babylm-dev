### BabySLM

To run an example of the BabySLM evaluation (for pre-computed probability scores) first dowload the dev data by running 
`./download_babyslm.sh`, and then  run `python -m src.babyslm.run_example`, or `pixi run run-babyslm-example`. 